name: ems_protocols
description: EMS protocol reference app with native text/vector pages recreated from the supplied Baxter Regional Medical Center protocol source.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.9.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/protocol_pages.json
    - assets/page_images/
